{
  "name": "hardhat-project",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run --network sepolia-testnet scripts/deploy.js",
    "deploy:test": "hardhat run --network sepolia-testnet scripts/deploy.js",
    "test": "hardhat test",
    "verify:sepolia": "hardhat verify --network sepolia-testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.1.3",
    "ethers": "^6.11.1",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
